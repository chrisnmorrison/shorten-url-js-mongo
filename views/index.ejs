<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="style.css">
    <title>URL Shortener</title>
</head>

<body>
    <h1 class='title'>URL Shortener</h1>

    <% if (locals.error) { %>
        <p class="error">
            <%= error %>
        </p>
        <% } %>

            <!-- <form action="/" method="post">
    <input type="url" name="url" id="url" placeholder="url here..">
    <input type="submit" value="create url">
    </form> -->

    <form action='/' method='post' class="form">
        <input type="url" name="url" id="url" class="form__field" placeholder="Enter URL to shorten" required/>
        <button type="submit" class="btn btn--primary btn--inside">Shorten!</button>
    </form>
</div>

</div>

            <div class="short-url">
                <% if (locals.short_url) { %>
                    <a href='<%= short_url %>'>
                        <%= short_url %>
                    </a>
                    <% } %>
            </div>

            <button class='btn btn--primary copy-link' onclick="copyToClipboard('.short-url')">Copy Link</button>

            <footer class="container__item container__item--bottom">
                <p>Created by <a href="https://chrismorrison.ca" target="_blank">Chris Morrison</a> of <a href="https://mesmerdesign.ca" 
                    target="_blank">Mesmer Design</a></p>
                </footer>
</body>

</html>